@model IEnumerable<MortgageSystem.ViewModel.CustomerLoanMortage>

@{
    ViewBag.Title = "FewInfoLoan";
    Layout = "~/Views/Shared/Loan_layout.cshtml";
}

<h2>Applied Loan Details</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LoanNumber)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.FName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LName)
        </th>


        <th>
            @Html.DisplayNameFor(model => model.Amount)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>

            <td>

                <a href="@Url.Action("ShowAllDetails","LoanOfficer", new { LoanNumber = item.LoanNumber },null)">

                    @Html.DisplayFor(modelItem => item.LoanNumber)
                </a>

            </td>

            <td>
                @Html.DisplayFor(modelItem => item.FName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LName)
            </td>


            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>


        </tr>
    }
    </table>

